[build]
  command   = "npm run build"
  publish   = "dist"

[build.environment]
  NODE_VERSION = "18"

# Pretty URLs — /about/ serves /about/index.html automatically
[[redirects]]
  from   = "/*"
  to     = "/404/index.html"
  status = 404
